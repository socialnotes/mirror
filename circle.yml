test:
  pre:
    - go get -u golang.org/x/tools/cmd/cover
    - go get -u github.com/axw/gocov/gocov
    - go get -u github.com/mattn/goveralls
  override:
    - goveralls -race -repotoken=$COVERALLS_TOKEN -service=circle-ci
